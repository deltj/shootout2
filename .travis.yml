language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
      dist: bionic
      addons:
        apt:
          packages:
            - libpcap0.8
            - libpcap0.8-dev
    
script:
  - mkdir build
  - cd build
  - cmake ..
  - make
